json.extract! equip, :id, :name, :set_name, :equip_type, :equip_subtype, :unlock, :starter, :event_only, :atk_scheme, :crit_scheme, :elem_scheme, :monster_id, :element_id, :created_at, :updated_at
json.url equip_url(equip, format: :json)
